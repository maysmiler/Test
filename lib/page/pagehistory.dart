import 'package:flutter/material.dart';

class PageHistory extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Test jaja',
      style: TextStyle(fontFamily: 'Exo'),
      ),
    );
  }
}